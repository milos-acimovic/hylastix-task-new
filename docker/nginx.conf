events {}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/page;
            index testpage.html;
        }

        location /auth/ {
            proxy_pass http://keycloak:8080/;
        }
    }
}